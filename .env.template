# OpenRouter (server-side only — never exposed to browser)
OPENROUTER_API_KEY=<your-openrouter-api-key>
OPENROUTER_MODEL=anthropic/claude-sonnet-4

# SurrealDB (server-side only — proxied through /api/db)
SURREALDB_URL=http://127.0.0.1:8000
SURREALDB_USER=root
SURREALDB_PASS=root
SURREALDB_NS=agentuidb
SURREALDB_DB=default
